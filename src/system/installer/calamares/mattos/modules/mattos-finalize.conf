---
# Reuses the Rust installer boot/profile policy after Calamares has created the
# target account and applied its locale/keyboard jobs.
dontChroot: true
timeout: 3600
verbose: true
script: /usr/libexec/mattos/calamares-target-executor --phase finalize --profile ${gs[packagechooser_profile]} --target ${ROOT}

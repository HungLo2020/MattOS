# MattOS uses self-contained PAM stacks, not Debian's common-* includes.
# Authenticate the administrator selected by Polkit; never permit unconditionally.
auth required pam_unix.so
account required pam_unix.so

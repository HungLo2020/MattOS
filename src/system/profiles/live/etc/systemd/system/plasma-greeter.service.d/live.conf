[Service]
# Keep the live profile explicit even if the unit's packaged ExecStart is
# already correct. This makes the live-vs-installed boundary auditable and
# prevents a vendor/default display-manager override from selecting COSMIC.
ExecStart=
ExecStart=/usr/bin/greetd --config /etc/greetd/plasma-live.toml
